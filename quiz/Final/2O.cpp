#include <bits/stdc++.h>
using namespace std;
int main(){
    int a , b , c , d;
    cin >> a;
    cin >> b;
    cin >> c;
    cin >> d;
    double x = -b / a;
    if(x = -d / c){
        cout << "NO";
    }
    else{
        cout << -b/a;
    }
}